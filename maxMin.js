// Find Maximum and Minimun Values of a List

// return min value
const min = (list) => {
  return Math.min.apply(null, list);
};

// return max value
const max = (list) => {
  return Math.max.apply(null, list);
};
